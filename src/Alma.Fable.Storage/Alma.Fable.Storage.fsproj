<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <OutputType>Library</OutputType>
        <PackageId>Alma.Fable.Storage</PackageId>
        <Version>6.0.0</Version>
        <RepositoryUrl>https://github.com/almacareer/prc-fable-storage.git</RepositoryUrl>
        <PackageProjectUrl>https://github.com/almacareer/prc-fable-storage</PackageProjectUrl>
    </PropertyGroup>

    <ItemGroup>
        <Compile Include="AssemblyInfo.fs" />
    </ItemGroup>

    <ItemGroup>
        <Compile Include="LocalStorage.fs" />
    </ItemGroup>

    <ItemGroup>
        <Content Include="*.fsproj; *.fs;" PackagePath="fable\" />
    </ItemGroup>

    <Import Project="..\..\.paket\Paket.Restore.targets" />

</Project>